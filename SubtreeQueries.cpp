#include<bits/stdc++.h>
using namespace std;
vector<int> tree;
int main()
{
	int n, q;
	cin >> n >> q;
	vector<int> val(n);
	tree.resize(n + 1);
	for (int i = 0; i < n; ++i)
		cin >> val[i];

	for (int i = 1; )
	}